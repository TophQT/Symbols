<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <!-- Tailwind CSS (via CDN for simplicity, or we can compile it) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Custom Tailwind Configuration -->
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'tech-dark': '#0b0e14',
              'tech-blue': '#3b82f6',
              'tech-accent': '#60a5fa',
              'tech-red': '#ef4444',
            },
            backdropBlur: {
              'xs': '2px',
            },
            animation: {
              'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
              'float': 'float 3s ease-in-out infinite',
            },
            keyframes: {
              float: {
                '0%, 100%': { transform: 'translateY(0)' },
                '50%': { transform: 'translateY(-10px)' },
              }
            }
          }
        }
      }
    </script>
    <!-- GSAP for animations -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <!-- Three.js -->
    <script async src="https://unpkg.com/es-module-shims@1.8.0/dist/es-module-shims.js"></script>
    <script type="importmap">
      {
        "imports": {
          "three": "https://unpkg.com/three@0.156.1/build/three.module.js",
          "three/addons/": "https://unpkg.com/three@0.156.1/examples/jsm/"
        }
      }
    </script>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap');
      
      body {
        font-family: 'Inter', sans-serif;
        background-color: #0b0e14;
        color: #ffffff;
        overflow-x: hidden;
      }
      
      .font-grotesk {
        font-family: 'Space Grotesk', sans-serif;
      }

      /* Glassmorphism */
      .glass {
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.05);
      }

      .glass-card {
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(8px);
        border: 1px solid rgba(255, 255, 255, 0.08);
        transition: all 0.3s ease;
      }

      .glass-card:hover {
        background: rgba(255, 255, 255, 0.05);
        border-color: rgba(59, 130, 246, 0.3);
        transform: translateY(-5px);
        box-shadow: 0 10px 30px -10px rgba(59, 130, 246, 0.2);
      }

      /* Animated Underline */
      .nav-link {
        position: relative;
      }
      .nav-link::after {
        content: '';
        position: absolute;
        width: 0;
        height: 2px;
        bottom: -4px;
        left: 0;
        background-color: #3b82f6;
        transition: width 0.3s ease;
      }
      .nav-link:hover::after {
        width: 100%;
      }

      /* Custom Scrollbar */
      ::-webkit-scrollbar {
        width: 8px;
      }
      ::-webkit-scrollbar-track {
        background: #0b0e14;
      }
      ::-webkit-scrollbar-thumb {
        background: #1e293b;
        border-radius: 4px;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: #3b82f6;
      }

      /* Hero Gradient */
      .hero-gradient {
        background: radial-gradient(circle at 50% 50%, rgba(59, 130, 246, 0.15) 0%, rgba(11, 14, 20, 1) 70%);
      }

      /* Barcode Scanner Laser */
      .laser-line {
        position: absolute;
        width: 100%;
        height: 2px;
        background: rgba(239, 68, 68, 0.8);
        box-shadow: 0 0 15px 2px rgba(239, 68, 68, 0.8);
        animation: scan 3s ease-in-out infinite;
      }

      @keyframes scan {
        0%, 100% { top: 0%; }
        50% { top: 100%; }
      }
    </style>
</head>
<body class="bg-tech-dark text-white selection:bg-tech-blue selection:text-white">
    <!-- Loading Screen -->
    <div id="loading-screen" class="fixed inset-0 z-[100] flex items-center justify-center bg-tech-dark transition-opacity duration-700">
        <div class="relative">
            <div class="w-16 h-16 border-4 border-tech-blue/20 border-t-tech-blue rounded-full animate-spin"></div>
            <div class="absolute inset-0 flex items-center justify-center">
                <div class="w-8 h-8 border-4 border-tech-accent/20 border-b-tech-accent rounded-full animate-spin" style="animation-direction: reverse;"></div>
            </div>
        </div>
    </div>

    <!-- Navigation Bar -->
    <header class="fixed top-0 left-0 w-full z-50 transition-all duration-300" id="main-header">
        <nav class="container mx-auto px-6 py-4 flex items-center justify-between">
            <!-- Logo -->
            <a href="/" class="flex items-center space-x-3 group">
                <div class="w-10 h-10 bg-tech-blue rounded-lg flex items-center justify-center transform group-hover:rotate-12 transition-transform duration-300 shadow-lg shadow-tech-blue/20">
                    <i data-lucide="scan-barcode" class="text-white w-6 h-6"></i>
                </div>
                <span class="text-xl font-bold font-grotesk tracking-tight text-white group-hover:text-tech-blue transition-colors">Symbol Sciences</span>
            </a>

            <!-- Desktop Menu -->
            <div class="hidden lg:flex items-center space-x-8">
                <a href="/" class="nav-link text-sm font-medium hover:text-tech-blue transition-colors">Home</a>
                <a href="/products" class="nav-link text-sm font-medium hover:text-tech-blue transition-colors">Products</a>
                <a href="/services" class="nav-link text-sm font-medium hover:text-tech-blue transition-colors">Services & Solutions</a>
                <a href="/updates" class="nav-link text-sm font-medium hover:text-tech-blue transition-colors">Updates</a>
                <a href="/about" class="nav-link text-sm font-medium hover:text-tech-blue transition-colors">About Us</a>
                <a href="/contact" class="px-5 py-2 bg-tech-blue hover:bg-tech-accent text-white rounded-full text-sm font-semibold transition-all shadow-lg shadow-tech-blue/20 transform hover:scale-105 active:scale-95">Contact Us</a>
            </div>

            <!-- Mobile Menu Toggle -->
            <button class="lg:hidden text-white focus:outline-none" id="mobile-menu-btn">
                <i data-lucide="menu" class="w-7 h-7"></i>
            </button>
        </nav>

        <!-- Mobile Menu Overlay -->
        <div id="mobile-menu" class="fixed inset-0 bg-tech-dark/95 backdrop-blur-xl z-[60] flex flex-col items-center justify-center space-y-8 opacity-0 pointer-events-none transition-all duration-300">
            <button class="absolute top-6 right-6 text-white" id="close-menu-btn">
                <i data-lucide="x" class="w-8 h-8"></i>
            </button>
            <a href="/" class="text-2xl font-bold hover:text-tech-blue">Home</a>
            <a href="/products" class="text-2xl font-bold hover:text-tech-blue">Products</a>
            <a href="/services" class="text-2xl font-bold hover:text-tech-blue">Services & Solutions</a>
            <a href="/updates" class="text-2xl font-bold hover:text-tech-blue">Updates</a>
            <a href="/about" class="text-2xl font-bold hover:text-tech-blue">About Us</a>
            <a href="/contact" class="px-8 py-3 bg-tech-blue text-white rounded-full text-lg font-semibold">Contact Us</a>
        </div>
    </header>
