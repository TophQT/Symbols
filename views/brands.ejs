<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              'tech-dark': '#0b0e14',
              'tech-blue': '#3b82f6',
              'tech-accent': '#60a5fa',
              'tech-red': '#ef4444',
            }
          }
        }
      }
    </script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap');
      
      body {
        font-family: 'Inter', sans-serif;
        background-color: #0b0e14;
        color: #ffffff;
        transition: background-color 0.3s ease, color 0.3s ease;
      }
      
      /* Light Mode Styles */
      body.light {
        background-color: #ffffff;
        color: #1f2937;
      }
      
      body.light .glass {
        background: rgba(31, 41, 55, 0.05);
        border: 1px solid rgba(31, 41, 55, 0.1);
      }
      
      body.light .glass-card {
        background: rgba(31, 41, 55, 0.03);
        border: 1px solid rgba(31, 41, 55, 0.08);
      }
      
      body.light .glass-card:hover {
        background: rgba(31, 41, 55, 0.05);
        border-color: rgba(59, 130, 246, 0.2);
      }
      
      body.light .text-white {
        color: #1f2937 !important;
      }
      
      body.light .text-white\/40 {
        color: rgba(31, 41, 55, 0.4) !important;
      }
      
      body.light .text-white\/60 {
        color: rgba(31, 41, 55, 0.6) !important;
      }
      
      body.light .text-white\/80 {
        color: rgba(31, 41, 55, 0.8) !important;
      }
      
      body.light .bg-white\/5 {
        background-color: rgba(31, 41, 55, 0.05) !important;
      }
      
      body.light .bg-white\/10 {
        background-color: rgba(31, 41, 55, 0.1) !important;
      }
      
      body.light .border-white\/5 {
        border-color: rgba(31, 41, 55, 0.05) !important;
      }
      
      body.light .border-white\/10 {
        border-color: rgba(31, 41, 55, 0.1) !important;
      }
      
      body.light .bg-tech-blue\/10 {
        background-color: rgba(59, 130, 246, 0.1) !important;
      }
      
      body.light .bg-tech-blue\/20 {
        background-color: rgba(59, 130, 246, 0.2) !important;
      }
      
      body.light .border-tech-blue\/20 {
        border-color: rgba(59, 130, 246, 0.2) !important;
      }
      
      body.light .border-tech-blue\/30 {
        border-color: rgba(59, 130, 246, 0.3) !important;
      }
      
      body.light #theme-toggle {
        background-color: rgba(31, 41, 55, 0.1);
        border-color: rgba(31, 41, 55, 0.2);
      }
      
      body.light #theme-toggle .bg-white {
        background-color: #f3f4f6;
      }
      
      /* Theme toggle active state */
      #theme-toggle.active {
        background-color: rgba(59, 130, 246, 0.2);
      }
      
      #theme-toggle.active .bg-white {
        transform: translateX(28px);
      }
      
      .font-grotesk {
        font-family: 'Space Grotesk', sans-serif;
      }

      .glass {
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.05);
      }

      .glass-card {
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(8px);
        border: 1px solid rgba(255, 255, 255, 0.08);
        transition: all 0.3s ease;
      }

      .glass-card:hover {
        background: rgba(255, 255, 255, 0.05);
        border-color: rgba(59, 130, 246, 0.3);
        transform: translateY(-5px);
        box-shadow: 0 10px 30px -10px rgba(59, 130, 246, 0.2);
      }
    </style>
</head>
<body class="bg-tech-dark text-white">
    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <aside class="w-64 glass border-r border-white/5 hidden md:flex flex-col fixed h-screen z-40">
            <div class="p-6">
                <a href="/" class="flex items-center group mb-8">
                    <div class="h-10 w-auto flex items-center justify-center transform group-hover:scale-110 transition-transform duration-300">
                        <i data-lucide="scan-barcode" class="text-tech-blue w-7 h-7"></i>
                    </div>
                </a>
                <div class="inline-flex items-center space-x-2 px-3 py-1 rounded-full bg-tech-blue/10 border border-tech-blue/20 text-tech-blue text-[10px] font-bold uppercase tracking-widest mb-4">
                    <span class="w-2 h-2 rounded-full bg-tech-blue"></span>
                    <span>System Online</span>
                </div>
            </div>
            <nav class="flex-grow space-y-1 px-4">
                <a href="/admin/dashboard" class="flex items-center gap-3 px-4 py-3 text-white/60 hover:text-white hover:bg-white/5 rounded-xl transition-all group">
                    <i data-lucide="layout-dashboard" class="w-5 h-5 group-hover:text-tech-blue transition-colors"></i>
                    <span class="font-medium">Dashboard</span>
                </a>
                <div class="space-y-1">
                    <div class="px-4 py-2 text-xs font-bold text-white/40 uppercase tracking-wider">Products</div>
                    <a href="/admin/products" class="flex items-center gap-3 px-4 py-3 text-white/60 hover:text-white hover:bg-white/5 rounded-xl transition-all group">
                        <i data-lucide="package" class="w-5 h-5 group-hover:text-tech-blue transition-colors"></i>
                        <span class="font-medium">Products</span>
                    </a>
                    <a href="/admin/products/brands" class="flex items-center gap-3 px-4 py-3 bg-tech-blue/10 text-tech-blue border border-tech-blue/20 rounded-xl transition-all">
                        <i data-lucide="bookmark" class="w-5 h-5"></i>
                        <span class="font-medium">Brands</span>
                    </a>
                    <a href="/admin/products/categories" class="flex items-center gap-3 px-4 py-3 text-white/60 hover:text-white hover:bg-white/5 rounded-xl transition-all group">
                        <i data-lucide="folder" class="w-5 h-5 group-hover:text-tech-blue transition-colors"></i>
                        <span class="font-medium">Categories</span>
                    </a>
                </div>
                <a href="/admin/settings" class="flex items-center gap-3 px-4 py-3 text-white/60 hover:text-white hover:bg-white/5 rounded-xl transition-all group">
                    <i data-lucide="settings" class="w-5 h-5 group-hover:text-tech-blue transition-colors"></i>
                    <span class="font-medium">Settings</span>
                </a>
            </nav>
            <div class="p-4 mt-auto">
                <a href="/admin/logout" class="flex items-center gap-3 px-4 py-3 text-tech-red hover:bg-tech-red/10 rounded-xl transition-all group">
                    <i data-lucide="log-out" class="w-5 h-5"></i>
                    <span class="font-medium">Logout</span>
                </a>
            </div>
        </aside>

        <!-- Main Content Area -->
        <main class="flex-grow md:ml-64 p-6 lg:p-10">
            <!-- Header -->
            <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 mb-10">
                <div class="space-y-1">
                    <h1 class="text-3xl font-bold font-grotesk text-white">Product Brands</h1>
                    <p class="text-white/40 text-sm">Manage your product brands and their descriptions</p>
                </div>
                <div class="flex items-center gap-3">
                    <!-- Theme Toggle -->
                    <button id="theme-toggle" class="relative w-14 h-7 bg-white/10 border border-white/20 rounded-full transition-colors duration-300 focus:outline-none focus:ring-2 focus:ring-tech-blue/50">
                        <div class="absolute top-0.5 left-0.5 w-6 h-6 bg-white rounded-full shadow-md transform transition-transform duration-300 flex items-center justify-center">
                            <i data-lucide="sun" class="w-3 h-3 text-tech-blue dark:hidden"></i>
                            <i data-lucide="moon" class="w-3 h-3 text-tech-blue hidden dark:block"></i>
                        </div>
                    </button>
                </div>
            </div>

            <!-- Success/Error Messages -->
            <% if (success) { %>
                <div class="mb-6 p-4 bg-green-500/10 border border-green-500/20 rounded-xl text-green-400 flex items-center gap-3">
                    <i data-lucide="check-circle" class="w-5 h-5"></i>
                    <span><%= success %></span>
                </div>
            <% } %>
            <% if (error) { %>
                <div class="mb-6 p-4 bg-red-500/10 border border-red-500/20 rounded-xl text-red-400 flex items-center gap-3">
                    <i data-lucide="alert-circle" class="w-5 h-5"></i>
                    <span><%= error %></span>
                </div>
            <% } %>

            <!-- Add Brand Form -->
            <div class="glass-card rounded-3xl p-8 border border-white/5 mb-10">
                <h2 class="text-xl font-bold font-grotesk text-white mb-6">Add New Brand</h2>
                <form action="/admin/products/brands" method="POST" enctype="multipart/form-data" class="space-y-6">
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label class="block text-sm font-medium text-white/80 mb-2">Brand Name</label>
                            <input type="text" name="name" required class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white placeholder-white/40 focus:outline-none focus:border-tech-blue/50 focus:bg-white/10 transition-all" placeholder="Enter brand name...">
                        </div>
                        <div>
                            <label class="block text-sm font-medium text-white/80 mb-2">Brand Logo</label>
                            <div class="relative">
                                <input type="file" name="logo" accept="image/*" required class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-medium file:bg-tech-blue/10 file:text-tech-blue hover:file:bg-tech-blue/20 transition-all">
                            </div>
                        </div>
                    </div>
                    <div>
                        <label class="block text-sm font-medium text-white/80 mb-2">Brand Description</label>
                        <textarea name="description" rows="3" required class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white placeholder-white/40 focus:outline-none focus:border-tech-blue/50 focus:bg-white/10 transition-all" placeholder="Enter brand description..."></textarea>
                    </div>
                    <div class="flex justify-end">
                        <button type="submit" class="px-6 py-3 bg-tech-blue hover:bg-tech-blue/80 text-white font-medium rounded-xl transition-all transform hover:scale-105 flex items-center gap-2">
                            <i data-lucide="plus" class="w-4 h-4"></i>
                            Add Brand
                        </button>
                    </div>
                </form>
            </div>

            <!-- Brands Grid -->
            <div class="space-y-6">
                <h2 class="text-xl font-bold font-grotesk text-white">Existing Brands</h2>
                <% if (brands.length === 0) { %>
                    <div class="glass-card rounded-3xl p-12 border border-white/5 text-center">
                        <i data-lucide="bookmark" class="w-16 h-16 text-white/20 mx-auto mb-4"></i>
                        <p class="text-white/40 text-lg">No brands found</p>
                        <p class="text-white/20 text-sm mt-2">Add your first brand using the form above</p>
                    </div>
                <% } else { %>
                    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                        <% brands.forEach(brand => { %>
                            <div class="glass-card rounded-2xl p-6 border border-white/5 hover:border-tech-blue/30 transition-all">
                                <div class="aspect-square mb-4 bg-white/5 rounded-xl overflow-hidden flex items-center justify-center">
                                    <img src="<%= brand.logo %>" alt="<%= brand.name %>" class="w-full h-full object-contain p-4">
                                </div>
                                <h3 class="text-white font-semibold text-lg mb-2"><%= brand.name %></h3>
                                <p class="text-white/60 text-sm line-clamp-3 mb-4"><%= brand.description %></p>
                                <div class="flex justify-between items-center">
                                    <div class="text-xs text-white/40">
                                        Created: <%= new Date(brand.createdAt).toLocaleDateString() %>
                                    </div>
                                    <div class="flex gap-2">
                                        <a href="/admin/products/brands/<%= brand._id %>/edit" class="px-3 py-1 bg-tech-blue/10 hover:bg-tech-blue/20 text-tech-blue text-xs font-medium rounded-lg transition-all flex items-center gap-1">
                                            <i data-lucide="edit" class="w-3 h-3"></i>
                                            Edit
                                        </a>
                                        <form action="/admin/products/brands/<%= brand._id %>/delete" method="POST" class="inline" onsubmit="return confirm('Are you sure you want to delete this brand? This action cannot be undone.')">
                                            <button type="submit" class="px-3 py-1 bg-tech-red/10 hover:bg-tech-red/20 text-tech-red text-xs font-medium rounded-lg transition-all flex items-center gap-1">
                                                <i data-lucide="trash-2" class="w-3 h-3"></i>
                                                Delete
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                <% } %>
            </div>
        </main>
    </div>
    <script>
        lucide.createIcons();
        
        // Theme Toggle Functionality
        const themeToggle = document.getElementById('theme-toggle');
        const body = document.body;
        
        // Check for saved theme preference or default to dark mode
        const savedTheme = localStorage.getItem('dashboard-theme') || 'dark';
        body.classList.toggle('light', savedTheme === 'light');
        themeToggle.classList.toggle('active', savedTheme === 'light');
        
        // Theme toggle event listener
        themeToggle.addEventListener('click', () => {
            const isLight = body.classList.toggle('light');
            themeToggle.classList.toggle('active', isLight);
            
            // Save theme preference
            localStorage.setItem('dashboard-theme', isLight ? 'light' : 'dark');
            
            // Re-initialize Lucide icons for theme change
            setTimeout(() => {
                lucide.createIcons();
            }, 100);
        });
        
        // Apply theme on page load
        if (savedTheme === 'light') {
            setTimeout(() => {
                lucide.createIcons();
            }, 100);
        }
    </script>
</body>
</html>
